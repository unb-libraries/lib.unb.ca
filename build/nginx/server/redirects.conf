# Check if the request is defined in a redirect map.
if ( $redirect_uri ) {
  return 301 $redirect_uri;
}

location = /guides {
  if ( $http_x_forwarded_host = 'lib.unb.ca' ) {
    rewrite ^/guides/(.*)  https://web.lib.unb.ca/guides/$1 break;
  }
}
