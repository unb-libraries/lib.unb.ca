langcode: en
status: true
name: 'Simple Group Permissions Paths'
vid: simple_group_paths
description: 'Permissions Paths'
hierarchy: 0
weight: 0

dependencies:
  module:
    - taxonomy
  enforced:
    module:
      - simple_group_path_perms
