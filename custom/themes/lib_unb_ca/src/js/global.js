/**
 * @file
 * Global lib_unb_ca utilities.
 *
 */
(function ($, Drupal) {

    'use strict';

    Drupal.behaviors.lib_unb_ca = {
        attach: function (context, settings) {
            // Custom lib_unb_va Drupal Javascript Behaviors.
        }
    };
})(jQuery, Drupal);
